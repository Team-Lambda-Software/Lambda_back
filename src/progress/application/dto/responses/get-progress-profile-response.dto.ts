//to-do Marked as "Provisional" within the common API specification.
export interface GetProgressProfileResponseDto
{
    latestCompletionPercent: number; //Is modeled as the completion percent of the latest course completed by user
    totalViewtimeInHours:number;
}